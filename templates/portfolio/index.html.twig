{% extends 'base.html.twig' %}

{% block title %}Mon Eportfolio{% endblock %}

{% block body %}

<div class="container text-center">
<h1 class="mt-4">Bienvenue sur mon Eportfolio</h1>

<!-- Nouveau bloc de texte avec fond transparent -->
<div class="content-box">
    <p class="titre-box">Tavernier Noa RT1 A2</p>
    <p class="text-box">Je suis un étudiant en première année de Réseaux et Télécommunications à l'IUT de Roanne. Passionné par les technologies et les innovations dans le domaine des réseaux, je vous invite à en savoir plus à propos de moi.</p>
</div>

<!-- Bouton d'orientation vers une autre page en dessous du rectangle -->
<a href="/passion" class="btn-custom">En savoir plus</a>

<!-- Pied de page -->
<div class="footer">
    <p>&copy; 2025 Tavernier Noa - Tous droits réservés.</p>
    <p>Mentions Légales</p>
</div>
</div>
{% endblock %}


{% block stylesheets %}
    <style>

        .content-box {
            background-color: rgba(255, 255, 255, 0.7); /* Fond blanc avec opacité de 70% */
            padding: 20px;
            border-radius: 10px; /* Bordures arrondies */
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2); /* Légère ombre pour un effet 3D */
            max-width: 80%; /* Limite la largeur à 80% de l'écran */
            margin: 50px auto 20px auto; /* 50px de marge en haut pour déplacer le rectangle plus bas */
        }

        h1 {
            font-size: 5vw; /* Taille de la police responsive */
            margin-top: 2rem;
            color: #fff; /* Texte en blanc */
            opacity: 0.9; /* Légèrement transparent */
        }

        .titre-box {
            color: #333; /* Texte foncé pour le contraste */
            line-height: 1.6; /* Espacement des lignes pour une meilleure lisibilité */
            font-size: 2rem;
        }

        .text-box {
            color: #333; /* Texte foncé pour le contraste */
            line-height: 1.6; /* Espacement des lignes pour une meilleure lisibilité */
            font-size: 1.2rem;
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            font-size: 1rem;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-decoration: none; /* Enlève le surlignement du texte */
            margin-top: 20px; /* Décale le bouton un peu plus bas */
        }

        .btn-custom:hover {
            background-color: #0056b3;
            cursor: pointer;
        }

        .footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px;
            position: relative;
            bottom: 0;
            width: 100%;
            margin-top: auto;
        }

        .footer a {
            color: #fff;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        .container {
            flex-grow: 1; /* Cette propriété fait en sorte que le contenu prenne tout l'espace restant */
            display: flex;
            flex-direction: column;
            justify-content: center; /* Centrage vertical du contenu */
            align-items: center; /* Centrage horizontal du contenu */
        }
    </style>
{% endblock %}

